spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: order-test
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka
